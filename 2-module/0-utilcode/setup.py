from setuptools import find_packages, setup
setup(
    name='utilcode', ## nómbre unico no indexado
    varsion='0.01',
    package=find_packages(),
    py_modules=['utilcode'] ## nombre del archivo .py
)